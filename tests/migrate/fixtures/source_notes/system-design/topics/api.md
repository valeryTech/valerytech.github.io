# API Design

> [!note] Rule of thumb
> Start with contracts.

> [!important]
> Keep the API contract stable.

> [!warning]+ Radiation hazard
> Preserve the custom title and ignore the fold sign while tracking $R_0$.

Inline math: $x = y + 1$.

Display math:

$$
f(x) = x^2
$$

Price stays literal at $10 a month.
Escaped prices stay literal at \$10 and \$20.
Shell code uses `$HOME` and `$PATH`; model math uses $R_D$.

Multiline display math:

$$
\begin{aligned}
a &= b \\
c &= d
\end{aligned}
$$

```md
> [!note] Example
> Keep this literal inside the code block.

Inline math stays literal here: $not_converted$.

$$
block_math_stays_literal
$$
```

See [[caching]] and [[#Local Heading]].

Markdown link: [Caching Doc](caching.md).

![[api-diagram.png]]
> API overview

## Local Heading

Anchor target.
